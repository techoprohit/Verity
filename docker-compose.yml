version: '3.8'

services:
  portal:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DB_PATH=/data/verity.db
    restart: unless-stopped
